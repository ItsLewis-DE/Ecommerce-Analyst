[project]
name = "project-aws"
version = "0.1.0"
description = "Add your description here"
readme = "README.md"
requires-python = ">=3.12"
dependencies = [
    "awscli-local>=0.22.2",
    "duckdb>=1.5.3",
    "numpy>=2.4.6",
    "pandas>=3.0.3",
    "pandera>=0.31.1",
    "pyarrow>=24.0.0",
]
